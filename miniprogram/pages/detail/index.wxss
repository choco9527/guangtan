@charset "UTF-8";
.detail-page .head-img {
  min-width: 100%;
  max-height: 20vh;
}
.detail-page .c-label {
  max-height: 0;
  height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease-in-out;
  background-color: #fff;
}
.detail-page .c-label .inner {
  padding: 4px 12px;
}
.detail-page .c-label.show {
  max-height: 50vh;
  height: auto;
}
.detail-page .infos {
  background-color: #fff;
  padding: 4px 12px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.detail-page #myMap {
  width: 100%;
  height: 150px;
  border-radius: 8px;
  overflow: hidden;
}
.detail-page .replies {
  background-color: #fff;
  padding: 12px;
}
.detail-page .replies .r-item {
  margin-bottom: 6px;
}
.detail-page .replies .r-item .user::after {
  content: "：";
  display: inline;
  color: inherit;
}

/*# sourceMappingURL=index.wxss.map */
