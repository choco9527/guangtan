<!--index.wxml-->
<view class="home-page container">
  <map id="homeMap"
       latitude="{{latitude}}"
       longitude="{{longitude}}"
       markers="{{markers}}"
       show-location
  />
</view>
